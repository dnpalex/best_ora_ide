#ifndef MAINVIEW_H
#define MAINVIEW_H

#include <QMainWindow>
#include "source/Views/mdisubview.h"
#include "source/Views/enumviewtype.h"

namespace Ui {
class MainView;
}

class MainView : public QMainWindow
{
    Q_OBJECT

public:
    explicit MainView(QWidget *parent = 0);
    ~MainView();

    void ShowSubView(MdiSubView *subView);

signals:

    void ShowSubView(SubViewType);

private:
    Ui::MainView *ui;
};

#endif // MAINVIEW_H
